kind: Kustomization
apiVersion: kustomize.config.k8s.io/v1beta1

namespace: elastic-stack-logging

resources:
- enrichment
- logstash
- namespace.yaml
# PDO-5984: opensearch operator manifests, OpenSearchCluster CR, security
# configs, storageclass, services, tenants, and cluster-info ConfigMap moved
# to the p1as-opensearch Helm chart in p1as-observability. CRDs are still
# owned by PCB (chart installs no CRDs).
- opensearch-operator/crd

configMapGenerator:
- name: elk-environment-variables
  envs:
  - env_vars

- name: p1as-bootstrap
