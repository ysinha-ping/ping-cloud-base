kind: Kustomization
apiVersion: kustomize.config.k8s.io/v1beta1

namespace: elastic-stack-logging

resources:
- ../../base/logging/

patchesStrategicMerge:
# PDO-5984: opensearch tier sizing (os-limits.yaml) moved to p1as-opensearch
# chart values; tier-specific overlay support is a follow-up MR.
- elastic-stack/logstash/hpa-limits.yaml
- elastic-stack/logstash/logstash-limits.yaml