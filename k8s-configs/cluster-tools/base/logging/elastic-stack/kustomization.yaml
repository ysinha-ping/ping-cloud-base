kind: Kustomization
apiVersion: kustomize.config.k8s.io/v1beta1

namespace: elastic-stack-logging

resources:
- enrichment
- logstash
- namespace.yaml
# PDO-5984: OpenSearch stack moved to p1as-opensearch Helm chart.
# CRDs now in p1as-opensearch/templates/crds.yaml, applied by Argo with ServerSideApply.

configMapGenerator:
- name: elk-environment-variables
  envs:
  - env_vars
