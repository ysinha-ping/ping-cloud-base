kind: Kustomization
apiVersion: kustomize.config.k8s.io/v1beta1

namespace: elastic-stack-logging

configMapGenerator:
  - name: fluent-bit-config
    files:
      - fluent-bit.conf
resources:
- fluentbit.yaml
- configs
